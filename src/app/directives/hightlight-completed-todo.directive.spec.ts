import { HightlightCompletedTodoDirective } from './hightlight-completed-todo.directive';

describe('HightlightCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
